# USER_JOURNEYS — as 17 jornadas do usuário final (estado após a missão)

Fonte dos gaps: `04-PRODUCT-GAPS.md` (auditoria da Fase 0). Cada jornada traz o estado **antes**, o que foi corrigido (commit) e como foi **verificado** (teste automatizado RED-first e/ou execução real contra instância isolada). Legenda: ✅ verificado · ☑ corrigido e coberto por teste · ⚠ residual documentado · ⛔ bloqueado por dependência externa.

| #   | Jornada                      | Antes (Fase 0)                                                                                                  | Depois (commits)                                                                                                                                                                                                        | Verificação                                                                                                       |
| --- | ---------------------------- | --------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------- |
| 1   | Instalar                     | Downloads/updater/feeds apontavam para o upstream; guia rápido só em EN                                         | Identidade do fork em fonte única `distribution.ts` (`e32178574`, `8c1679002`, `2c2826c93`); gate npm por `repository.url`; quick start pt-BR com "primeiro uso em 5 passos" (`de93f171d`)                              | ☑ suítes de identidade/packaging; `check:pack-artifact`/`check:install-upgrade` (ver `TEST_MATRIX.md`)            |
| 2   | Abrir (Electron)             | Janela ficava na página em branco do Chromium se o servidor não respondesse; sem `did-fail-load`                | `electron/lib/loadFailure.js`: página estática "Aguardando o servidor…" + poll de readiness + reload (`735ab11c3`); tray/notificações em pt/en (`b10267d8d`)                                                            | ☑ `electron-load-failure-j2` (4), `electron-tray-strings-i3` (3); 137 testes Electron PASS                        |
| 3   | Concluir onboarding          | Erros **nunca apareciam** (`errorMessage` não renderizado); inputs sem nome acessível; grid quebrava no celular | `role="alert"` nos passos (`3f524cbdb`), timeout 15 s no teste de conexão + Retry (`d2fd682e9`), `aria-label` nos 4 inputs (`7ef97c63c`), `grid-cols-2 sm:grid-cols-3` (`2e421d317`), Caps Lock traduzido (`b3b4ce963`) | ☑ `onboarding-error-visible` (3), `onboarding-test-timeout`; UI vitest                                            |
| 3b  | Senha padrão `CHANGEME`      | Login sempre exibia a dica da senha padrão                                                                      | Dica só quando `usingDefaultPassword` (servidor compara o hash ativo) e vira aviso para trocar (`ec4939423`)                                                                                                            | ☑ `login-bootstrap-route` +2, `login-default-password-hint`                                                       |
| 4   | Proteger o painel            | Ligar "Exigir login" sem senha = auto-lockout                                                                   | 400 `PASSWORD_REQUIRED_TO_ENABLE_LOGIN` no servidor; Security tab pede a senha inline (`c3dbff45a`)                                                                                                                     | ☑ `security-tab-enable-login`, rota `require-login`                                                               |
| 5   | Cadastrar provedor           | Import por arquivo com 3 chaves sem tradução em pt                                                              | Chaves adicionadas (`1a083e3bb`)                                                                                                                                                                                        | ☑ `check-ui-keys-coverage` 41/41                                                                                  |
| 6   | Testar conexão               | Erro técnico cru na UI (`code`/objeto)                                                                          | `presentApiError` (código → mensagem i18n, detalhe em "Ver detalhes"; crash "Objects are not valid as a React child" reproduzido e corrigido) (`73e188465`)                                                             | ☑ `security-tab-errors`, `present-api-error`                                                                      |
| 7   | Visualizar modelos           | Sem gap funcional                                                                                               | —                                                                                                                                                                                                                       | ✅ `/v1/models` lista o provedor custom com chave; 401 tipado sem chave (`compat-isolated`)                       |
| 8   | Copiar URL e API key         | Chaves antigas reveláveis por `/api/keys/{id}/reveal`; falha ao copiar silenciosa                               | Reveal-once (`f9ec8e0ed`); `useCopyToClipboard.failed` + "Falha ao copiar — selecione…" nos botões críticos (`a57157e79`)                                                                                               | ☑ `use-copy-to-clipboard-feedback-u6` (3); reveal removido com teste                                              |
| 9   | Configurar Codex             | Guia só em EN; denso                                                                                            | Seção pt-BR (chave em `OMNIROUTE_API_KEY`, `omniroute launch-codex --model auto`) no quick start (`de93f171d`)                                                                                                          | ✅ `/v1/responses` JSON+SSE com `response.completed` (`compat-isolated`)                                          |
| 10  | Configurar Claude Code       | Sem guia pt-BR                                                                                                  | Seção pt-BR (`omniroute run claude`, `ANTHROPIC_BASE_URL` sem `/v1`, `ANTHROPIC_AUTH_TOKEN`) (`de93f171d`)                                                                                                              | ✅ `/v1/messages` JSON (`type/role/content/usage`) + SSE (`message_start…message_stop`) (`compat-isolated`)       |
| 11  | Enviar solicitação           | Cancelar um stream **não parava o provedor** (continuava gerando/cobrando) — descoberto na Fase 6               | R-21: abort do cliente encaminhado ao upstream após o início do stream (`bdc34c38e`)                                                                                                                                    | ✅ cancelamento observado como fechamento prematuro no mock upstream; `upstream-abort-after-start-f6` (RED-first) |
| 12  | Entender erros               | Erros de API vazavam crus                                                                                       | `presentApiError` (J6); erros tipados nos endpoints `/v1/*` (prefixo desconhecido → 4xx JSON; upstream 500 → JSON)                                                                                                      | ✅ `compat-isolated` "typed errors"                                                                               |
| 13  | Reiniciar                    | Reload fixo de 3 s → página de conexão recusada                                                                 | `waitForServerRestart` (espera cair, depois `/api/health/ping` 2xx, 90 s); timeout mantém o botão "Recarregar" (`005a8537c`)                                                                                            | ☑ `wait-for-server-ready-u7` (5)                                                                                  |
| 14  | Fechar e reabrir             | Tray/notificações só em EN; órfãos POSIX; sem shutdown de schedulers                                            | Tray en/pt (`b10267d8d`); `detached`+`kill(-pid)`+`server.pid`+`reapOrphanServer` (`36c6aa58f`); `stopBackgroundWork()` antes do drain (R-6)                                                                            | ☑ `electron-processtree-orphans-r12`, `graceful-shutdown-stops-background-work-r6`                                |
| 15  | Atualizar                    | Feed do upstream; sem snapshot antes de instalar                                                                | Updater → fork (`e32178574`); **snapshot pré-update** DB+WAL/SHM+`server.env`+`.env`+prefs, 3 retidos, em ambos os caminhos (`ad95c16ad`); restauração em `ROLLBACK.md` §2.3                                            | ☑ `electron-pre-update-snapshot-e5` (5) · ⛔ E-4 code-signing (certificados do operador)                          |
| 16  | Backup e restauração         | Restore point de migração sem nome/handle aberto                                                                | R-1: restore point nomeado e verificado por SHA-256, handle fechado antes do throw; `audit/ROLLBACK.md`; `DATABASE_GUIDE.md`                                                                                            | ☑ `db-migration-rollback-restore-point`, `db-migration-duplicate-column-partial-r4`                               |
| 17  | Desinstalar sem perder dados | `uninstall:full` apagava `~/.omniroute` sem confirmação; `uninstall` não dizia onde ficaram os dados            | `ERASE` no TTY ou `--yes`; senão exit 1 sem apagar; pasta preservada informada; docs EN + pt-BR (`bfab445eb`)                                                                                                           | ☑ `uninstall-full-confirmation-j17` (5)                                                                           |

## Acessibilidade e responsividade (WCAG AA — escopo executado)

- Botões só-ícone nomeados (menu mobile, setas de prioridade, fechar banner) com `aria-label`/`title` e glifo `aria-hidden` (`8ff49a923`).
- Inputs do onboarding com nome acessível (`7ef97c63c`); demais ~53 inputs só-placeholder registrados como IMPROVEMENT em `04` (sem regressão de comportamento).
- Confirmações destrutivas migradas de `confirm()` nativo (não traduzido, sem foco gerenciado) para o `ConfirmModal` compartilhado — 35 sites (`d4d8918ad`).
- Grids do onboarding responsivos em 375 px (`2e421d317`).
- Suítes `tests/e2e/a11y*.spec.ts` (Playwright/axe) existem no repositório; execução local depende do Playwright + navegador (ver `TEST_MATRIX.md`).

## Execução real (como usuário) — o que foi exercitado

- Instância isolada (`npm run test:compat`): criar chave, listar modelos, chat JSON/SSE, Claude Code (`/v1/messages`), Codex (`/v1/responses`), cancelar stream, erros tipados, MCP HTTP — 8/8.
- Fluxos de UI cobertos por vitest jsdom (onboarding, login, security tab, api-manager, endpoints, combos, batch, quota-share, model-select, ConfirmDialog) — ver `TEST_MATRIX.md`.
- Não executado por dependência externa: login em provedor real, atualização real do Electron (exige release publicada + assinatura), E2E Playwright completo com navegador (ver `TEST_MATRIX.md`).

## Addendum — verificação do Auditor C no produto (rodadas 1 e 2)

O Auditor C percorreu o produto no navegador (instância isolada, pt-BR): login + dica CHANGEME, chave reveal-once, exclusão com modal traduzido, `/v1/models` com 401 tipado, Reiniciar com espera, mobile 375 px — verificados. Os três HIGH que ele encontrou nas jornadas 1, 6 e 11/13 (C-01 live WebSocket preso à porta 20128, C-02 guias mandando instalar o upstream e prometendo copiar a chave depois, C-03 "Endpoint <path>" no teste de conexão) foram corrigidos e **re-verificados no produto** na rodada 2, junto com C-04 (pt-BR nas telas principais), C-05 (`/v1/models` sem CLIs ausentes), C-07 (caminhos reais) e o assistente inicial que dizia "conexão bem-sucedida" para host morto. Residuais documentados em `04-PRODUCT-GAPS.md` (addendum) e `FINAL_THREE_AGENT_REVIEW.md` §3/§5: C-06 (assistente inalcançável com `INITIAL_PASSWORD` — agora descrito nos guias), C-08 (a11y de ícones/switches), C-09 (layout ≤ 1000 px), C-10/C-12…C-17 e D-2/D-5/D-7 (LOW).
