- The desktop release pipeline now signs the installers as soon as the owner adds the signing repository secrets — a Developer ID certificate plus App Store Connect or Apple ID notarization on macOS, an Authenticode certificate or Azure Trusted Signing on Windows — and keeps producing the unsigned build, green, while they are absent; the exact secrets and the re-attach command are in `docs/guides/ELECTRON_GUIDE.md` (#18)
