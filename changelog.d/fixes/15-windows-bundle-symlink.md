- The Windows leg of the desktop release no longer fails on a symlink that only looks different: the shared web bundle is packed on Linux, and a `.bin` shim whose target is an absolute POSIX path reads back on Windows with a drive prefix and backslashes, so manifest verification reported a mismatch that was about the platform rather than the link. Both sides are normalized for the comparison, and a link that genuinely points elsewhere still fails (#15)
