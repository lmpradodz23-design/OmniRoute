- The Browser Use domain allowlist is now configurable: `GET`/`PUT /api/browser/allowlist` (admin-scoped writes; hosts are normalized and IP literals, wildcards, schemes, paths and ports are refused) and a "Browser Use domain allowlist" card in Settings → Security, so `POST /api/browser/check` without `allowedDomains` uses a persisted list instead of one that was always empty (#19)
